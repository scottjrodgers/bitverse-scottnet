/** @param {NS} ns */
export async function main(ns) {
  // 1. Start comp sci.  2GB x 16
  ns.singularity.universityCourse("Rothman University", "Computer Science". false);
  
  // 2. execute "nukem.js"

  // 3. execute "prep_controller.js"

  // 4. buy TOR router.  2GB x 16
  ns.singularity.purchaseTor();

  // 5. as I can afford each of the hack programs, if I don't already have it, purchase it
  // 
}